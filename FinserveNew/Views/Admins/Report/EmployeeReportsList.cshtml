@model IEnumerable<dynamic>
@{
    ViewData["Title"] = "Employee Reports";
}

<div class="container py-4">
    <h2 class="mb-3">Employee Reports</h2>
    <p class="text-muted">Browse submitted employee dashboard reports. Click a row to open.</p>

    <div class="table-responsive">
        <table class="table table-striped align-middle">
            <thead>
                <tr>
                    <th>Submitted By</th>
                    <th>Employee ID</th>
                    <th>Created</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model == null || !Model.Any())
                {
                    <tr>
                        <td colspan="4" class="text-center text-muted">No employee reports yet.</td>
                    </tr>
                }
                else
                {
                    foreach (var item in Model)
                    {
                        string submitter = item.Submitter ?? "-";
                        string employeeId = item.EmployeeId ?? "-";
                        DateTime created = item.CreatedAt;
                        string file = item.File;
                        <tr>
                            <td>@submitter</td>
                            <td>@employeeId</td>
                            <td>@created.ToString("dd MMM yyyy HH:mm")</td>
                            <td>
                                <a class="btn btn-sm btn-primary" href="@Url.Action("ViewEmployeeReport", "Home", new { file = file })">Open</a>
                                <a class="btn btn-sm btn-secondary" download href="@Url.Content($"~/reports/employee/{file}")">Download JSON</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>


